export enum authRoutes {
  REGISTER = "/register",
  LOGIN = "/login",
}
export enum postRoutes {
  CREATE = "/create",
  UPDATE = "/update",
  DELETE = "/delete",
  GETALL = "/getAll",
  GETSINGLE = "/getsingle",
}
export enum commentRoutes {
  CREATE = "/add",
  UPDATE = "/update",
  DELETE = "/delete",
  GETALL = "/getall",
}
export enum likeRoutes {
  CREATE = "/add",
  GETALL = "/get",
}
export enum allRoutes {
  USER = "/user",
  POST = "/post",
  COMMENT = "/comment",
  LIKE = "/like",
}
export enum mainRoutes {
  API = "/api",
}
